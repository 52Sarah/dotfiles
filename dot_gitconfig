[alias]

  alias = !bash -li -c 'git-alias'

  # -C - detect copied lines, even from other files in the same commit
  # -M - detect moved lines, even from other files in the same commit
  # -w - ignore whitespace
  bl = blame -C -M -w

  brcp = branch -C
  brmv = branch -M
  brrm = branch -D
  brsu = branch --set-upstream-to

  b = branch --list --ignore-case --sort=-committerdate --format="%(if)%(HEAD)%(then)%(color:magenta)*%(else)%(color:yellow)\\ %(end)%(objectname:short)\\ \\ %(color:reset)%(if)%(HEAD)%(then)%(color:magenta)%(end)%(align:12,left)%(committerdate:relative)%(end)\\ \\ %(if)%(HEAD)%(then)%(else)%(if:equals=refs/remotes)%(refname:rstrip=-2)%(then)%(color:cyan)%(end)%(end)%(refname:short)%(color:white)\\ %(if)%(upstream)%(then)%(align:2,left)[%(upstream:trackshort)]%(end)%(end)%(color:reset)"
  br = branch --list --ignore-case --sort=-committerdate --column=never --format="%(if)%(HEAD)%(then)%(color:magenta)*%(else)%(color:yellow)\\ %(end)%(objectname:short)\\ \\ %(color:reset)%(if)%(HEAD)%(then)%(color:magenta)%(end)%(align:14,left)%(committerdate:format:%D\\ %H:%M)%(end)\\ \\ %(align:20,left)%(authorname)%(end)\\ %(align:60,left)%(if)%(HEAD)%(then)%(else)%(if:equals=refs/remotes)%(refname:rstrip=-2)%(then)%(color:cyan)%(end)%(end)%(refname:short)%(if)%(upstream)%(then)\\ %(color:white)%(align:2,left)[%(upstream:trackshort)]%(end)%(color:reset)%(end)%(end)\\ %(contents:subject)"
  
  bra = branch --list --ignore-case --sort=-committerdate --column=never --format="%(if)%(HEAD)%(then)%(color:magenta)*%(else)%(color:yellow)\\ %(end)%(objectname:short)\\ \\ %(if)%(HEAD)%(then)%(color:magenta)%(else)%(color:reset)%(end)%(align:14,left)%(committerdate:format:%D\\ %H:%M)%(end)\\ \\ %(align:20,left)%(authorname)%(end)\\ %(align:60,left)%(if)%(HEAD)%(then)%(color:reset)%(else)%(if:equals=refs/remotes)%(refname:rstrip=-2)%(then)%(color:cyan)%(else)%(color:reset)%(end)%(end)%(refname:short)%(color:cyan)%(if)%(upstream)%(then)\\ %(upstream:track)%(end)%(color:reset)%(end)\\ %(contents:subject)"

#  xbra = branch --list --ignore-case --sort=-committerdate --column=never --format="
#    %(if)%(HEAD)%(then)
#      %(color:magenta)*
#    %(else)
#      %(color:yellow)
#      \\ 
#    %(end)
#    %(objectname:short)
#    \\ \\
#    %(if)%(HEAD)%(then)
#      %(color:magenta)
#    %(else)
#      %(color:reset)
#    %(end)
#    %(align:14,left)
#      %(committerdate:format:%D\\ %H:%M)
#    %(end)
#    \\ \\ 
#    %(align:20,left)%(authorname)%(end)
#    \\ 
#    %(align:60,left)
#      %(if)%(HEAD)%(then)%(else)
#        %(if:equals=refs/remotes)%(refname:rstrip=-2)%(then)
#          %(color:cyan)
#        %(else)
#          %(color:reset)
#        %(end)
#      %(end)
#      %(refname:short)
#      %(color:white)
#      %(if)%(upstream)%(then)
#        \\ 
#        %(upstream:track)
#      %(end)
#      %(color:reset)
#    %(end)
#    \\
#    %(color:cyan) 
#    %(if)%(upstream)%(then)
#      [%(upstream:short)]
#      \\ 
#    %(end)
#    %(color:reset)
#    \\ 
#    %(contents:subject)
#"
 
  co =    checkout
  com =   checkout master
  cod =   checkout develop

  cp =    cherry-pick --edit
  
  d =     diff
  ds =    diff --stat-graph-width=20
  dss =   diff --shortstat
  dds =   diff --dirstat
  dt =    difftool --dir-diff

  fet =   fetch --progress --verbose
  fett =  fetch --tags

  lo = log -20 --abbrev=6 --date='format:%D'
  loo = log -20 --abbrev=short --date=human --use-mailmap
  looo = log -20 --abbrev=short --date=human --use-mailmap --format='%<(7)%C(yellow)%h  %C(reset)%<(22)%aN  %C(white)%<(8)%ad %C(white)%<(12)%ar %C(bold cyan)%d%n  %C(reset)%s%C(reset)'
  loooo = log -20 --abbrev=short --date=human --no-use-mailmap --stat --format='%<(7)%C(yellow)%h  %C(reset)%<(22)%aN  %C(white)%ad  %C(white)%<(12)%ar%C(bold cyan)%d%n  %C(white)%s%n  %C(reset)%b%C(reset)'

  # Enhance git checkout with post-update hook, which pushes branch names into $git_dir/HEADS
  # for this function to pop.
  popb = !git-popb.sh

  pff   = pull --ff-only --progress
  pullf = pull --ff-only --progress
  pullr = pull --rebase --progress

  pushup = push --set-upstream origin HEAD

  rab =   rebase --abort
  rc =    rebase --continue

  st = status --short
  stt = status --column=dense --no-show-stash
  # Show files in columns
  sttt = status --column=nodense --show-stash
  # Show files in columns, include ignored files; show diffs
  stttt = status --column=nodense --show-stash --ignored=traditional --verbose

[blame]
  coloring = highlightRecent  # options: repeatedLines, highlightRecent, NONE
  date = format:%a %D %H:%M  # Thu 10/11/18 18:31

[branch]
  autoSetupMerge  = false
  autoSetupRebase = always
  sort = -committerdate

[checkout]
  defaultRemote = origin

# From: https://git-scm.com/docs/git-config#Documentation/git-config.txt-colorui
# Values are always|NEVER|auto, COLUMN|row|plain, dense|NODENSE
[column]
  #! ui = never,column,nodense
  branch = auto
  status = always  # nodense seems to have no effect on status (set here or on .ui)
  tag = always

[commit]
  verbose = true

[core]
  abbrev = 8
  #! commentChar = #
  editor = subl
  excludesFile = ~/.gitignore
  hooksPath = ~/.git_hooks
  pager = less
  whitespace = -trailing-space,tab-in-indent,tabwidth=2
  # whitespace defaults: space-before-tab, -indent-with-non-tab, -cr-at-eol

[credential]
  helper = osxkeychain

# See: https://git-scm.com/docs/git-config#Documentation/git-config.txt-diffdirstat  
#   .dirstat parameters: files|CHANGES|lines, cumulative|NONCUMULATIVE, 3 (cutoff %)
#   .relative 'true' only performs diff in current directory and below
#   .renames 'copies' detects renames and copies
#   .algorithm 'minimal' is slower but generates the smallest correct diff
[diff]
  algorithm = minimal
  colorMoved = true
  context=2  # default: 3
  dirstat = lines,cumulative,0
  relative = true
  renames = copies
  # statGraphWidth = 20

[fetch]
  output = compact  # branchName -> origin/* instead of origin/branchName
  #! prune = true
  #! showForcedUpdates = true
  writeCommitGraph = true

[grep]
  fallbackToNoIndex = true
  lineNumber = true
  patternType = extended

[log]
  abbrevCommit = true  #! false
  #! conflictStyle = merge
  date = human #!iso
  decorate = short     # none|short|full|AUTO
  follow = true
  #! mailmap = true

[mailmap]
  file = /Users/dad/.mailmap

[merge]
  #! autoStash = false
  directoryRenames = true  # default: conflict
  ff = only
  guitool = bc
  renames = copies
  #! stat = true
  tool = bc
  #! verbosity = 2  # 2 is conflicts and file changes

[mergetool "bc"]
  trustExitCode = true

[pull]
  ff = only
  rebase = true

[push]
  #! default = simple  # upstream unless name is different
  useForceIfIncludes = true  # minimize explicit forces

[rebase]
  #! autoSquash = false
  #! autoStash = false
  missingCommitsCheck = warn
  stat = true

[remote]
  #! pushDefault = origin

[remote "master"]
  pushDefault = PULL_REQUEST_YO

[reset]
  #! quiet = false

[stash]
  #! showStat = true

# by default, full "status" command is complete; "st" alias is terse
[status]
  aheadBehind = true
  branch = true  # only relevant for short=true
  displayCommentPrefix = true
  relativePaths = false  # TRUE: relative to PWD; false: to repo root
  renames = copies
  #! short = false
  showStash = true
  #! showUntrackedFiles = normal  # options: no|NORMAL|all

[user]
  name = Todd Pierzina
  email = tpierzina@accertify.com


#
### available colors, attributes
#
#
# normal, black, red, green, yellow, blue, magenta, cyan, or white
# bold, dim, ul (underline), blink, and reverse

[include]
  path = .gitconfig__colors
  
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
